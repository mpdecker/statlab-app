{
  "name": "statlab-monorepo",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "app"
  ],
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "dev": "pnpm dev -w app",
    "build": "pnpm --filter statlab build && pnpm --filter statlab-app build",
    "build:lib": "pnpm --filter statlab build",
    "build:app": "pnpm --filter statlab-app build",
    "test": "pnpm --filter statlab test && pnpm --filter statlab-app test",
    "test:lib": "pnpm --filter statlab test",
    "test:app": "pnpm --filter statlab-app test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "coverage": "pnpm exec vitest run --coverage -w statlab",
    "barrel": "node scripts/gen-barrel.mjs",
    "docs": "node scripts/statlab.mjs",
    "docs:list": "node scripts/statlab.mjs list",
    "docs:search": "node scripts/statlab.mjs search",
    "docs:generate": "node scripts/document.mjs",
    "reference:generate": "node scripts/dump-fixtures.mjs && python scripts/gen-reference.py"
  }
}
